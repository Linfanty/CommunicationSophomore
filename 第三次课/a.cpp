#include <cstdio>
#include <iostream>
#include <algorithm>
using namespace std;
typedef long long int ll;
const int maxn = 105;


int main()
{
    int a, b, c;
    while( scanf("%d/%d/%d", &a, &b, &c) != EOF)
    {
        printf("%d %d %d\n", a, b, c);
    }
}
