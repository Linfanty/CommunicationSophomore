#include <stdio.h>
#include <bits/stdc++.h>
using namespace std;

int main()
{

    string line;
    while( getline(cin, line) )
    {
        if(line.size() == 0)
            break;
        cout << line << endl;
    }

}

