#include <cstdio>
#include <iostream>
#include <algorithm>
#include <vector>
#include <bits/stdc++.h>
using namespace std;

int main()
{
    freopen("in.txt", "r", stdin);
    freopen("out.txt", "w", stdout);
    int a, b;
    cin >> a >> b;
    cout << a + b << endl;

/**
    vector<int> ve;
    ve.push_back(5);
    ve.push_back(6);
    ve.push_back(4);
    cout << ve.size() <<endl;
    ve.pop_back();
    vector<int>:: iterator it;
    for(it = ve.begin(); it != ve.end(); it++)
        cout << (*it) << ' ';
*/
    return 0;
}

